// Organization layer — org switcher + organization-scope pages. Auto-discovered
// by Nuxt; $meta.name generates the #layers/organization alias.
export default defineNuxtConfig({
  $meta: {
    name: 'organization',
  },
})
