// Admin layer — platform-scope (system admin) pages. Auto-discovered by Nuxt;
// $meta.name generates the #layers/admin alias.
export default defineNuxtConfig({
  $meta: {
    name: 'admin',
  },
})
