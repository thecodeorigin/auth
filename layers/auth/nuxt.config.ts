// Auth layer — auth surfaces, account scope, API keys, session/user-menu and
// impersonation components. Auto-discovered by Nuxt because it lives under
// <root>/layers/. The $meta.name generates the #layers/auth alias.
export default defineNuxtConfig({
  $meta: {
    name: 'auth',
  },
})
