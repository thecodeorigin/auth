{
  "name": "@thecodeorigin/auth",
  "type": "module",
  "version": "0.0.1",
  "description": "THECODEORIGIN Authentication Portal client module",
  "repository": "https://github.com/thecodeorigin/auth",
  "exports": {
    ".": {
      "types": "./dist/types.d.mts",
      "import": "./dist/module.mjs"
    },
    "./contract": {
      "types": "./dist/contract/index.d.mts",
      "import": "./dist/contract/index.mjs"
    },
    "./server": {
      "types": "./src/runtime/server/index.d.mts",
      "import": "./src/runtime/server/index.mjs"
    }
  },
  "main": "./dist/module.mjs",
  "typesVersions": {
    "*": {
      ".": [
        "./dist/types.d.mts"
      ],
      "contract": [
        "./dist/contract/index.d.mts"
      ],
      "server": [
        "./src/runtime/server/index.d.mts"
      ]
    }
  },
  "files": [
    "dist"
  ],
  "workspaces": [
    "playground"
  ],
  "scripts": {
    "prepack": "nuxt-module-build build",
    "dev": "npm run dev:prepare && nuxt dev playground",
    "dev:build": "nuxt build playground",
    "prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxt prepare playground",
    "release": "npm run lint && npm run test && npm run prepack && changelogen --release && npm publish && git push --follow-tags",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:types": "vue-tsc --noEmit && cd playground && vue-tsc --noEmit"
  },
  "dependencies": {
    "@casl/ability": "^6.8.1",
    "@casl/vue": "^2.2.6",
    "@nuxt/kit": "^4.4.2",
    "defu": "^6.1.4",
    "h3": "^1.14.0",
    "ofetch": "^1.4.1",
    "ufo": "^1.5.4",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@nuxt/devtools": "catalog:",
    "@nuxt/eslint-config": "catalog:",
    "@nuxt/module-builder": "catalog:",
    "@nuxt/schema": "catalog:",
    "@nuxt/test-utils": "catalog:",
    "@types/node": "catalog:",
    "changelogen": "catalog:",
    "eslint": "catalog:",
    "nuxt": "catalog:",
    "typescript": "catalog:",
    "unbuild": "catalog:",
    "vitest": "catalog:",
    "vue-tsc": "catalog:"
  }
}
